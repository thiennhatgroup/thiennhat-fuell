# Handoff / Context — App Tồn kho Xăng dầu

Đọc file này ĐẦU TIÊN khi mở session mới, rồi mở PRD/glossary/issues nếu cần chi tiết.

## 1. Dự án là gì
Web app tồn kho xăng dầu cho Thiên Nhật: nhập **bơm** (BOM) cho xe, **nhập** dầu về téc (NHAP), **tịnh téc/kiểm kê** khi lệch, **tồn kho thời gian thực**, **báo cáo tiêu hao tháng** cho kế toán. Port từ `File nhap lieu xang dau_v11.xlsx` + `appscript code.rtf`.
- Kiến trúc: **giống hệt `thiennhat-supabase`** — Supabase (Postgres) deny-by-default, mọi ghi qua RPC `SECURITY DEFINER` + `require_permission()`; frontend một file `public/index.html` (PWA) host GitHub Pages; auth email+PIN.
- Repo: `github.com/thiennhatgroup/thiennhat-fuell`. Supabase ref `zaipiepimqkbulaiaupe`.

## 2. Đã quyết (từ grilling — chi tiết ở PRD)
Vai trò `ThuKho`/`KeToan`/`Admin`. Đối chiếu chéo do **thủ kho khác người nhập** (per-phiếu; duyệt→vào tồn, từ chối→trả về sửa). Ảnh **bắt buộc 2 ảnh/phiếu bơm** (đồng hồ bơm + công-tơ-mét), Storage riêng tư, schema **sẵn-cho-OCR** (OCR bật phase sau). Phiếu bơm: Nháp→Submit cả lô cuối ngày→Chờ đối chiếu→Đã duyệt/Trả về; rút lại khi chưa đối chiếu. NHAP cũng có ảnh+đối chiếu; **TINH_TEC do Kế toán chốt**. Tịnh téc + **kiểm kê** hợp nhất một cơ chế, phân bổ chênh lệch cho xe theo **lít-trong-kỳ** (giữ nguyên công thức cũ). Tồn kho `= tồn đầu + nhập − xuất − tịnh âm + tịnh dương`. Bơm ngoài không trừ tồn téc. Thêm/bớt xe (soft-delete). Báo cáo **tái hiện sheet cũ 1-1** + tải Excel. Cảnh báo reorder + push PWA. Online-only. Migrate toàn bộ ~5.997 dòng DATA_GOC dạng legacy đã duyệt.

## 3. Trạng thái build
- **S0 (nền tảng) — XONG & đã push `main`** (commit "S0…"): `supabase/migrations/0001_schema..0005_admin_users`, `public/index.html` (login email+PIN, shell điều hướng theo vai trò, màn Tài khoản chạy thật), workflows, config.js đã điền URL+anon.
- **Go-live S0 đang chờ người dùng**: đặt 3 Actions secrets → chạy workflow "Deploy Supabase migrations" → bật Pages (gh-pages) → tạo Admin (Auth → Users, password `tn-pin::<PIN>`) + chạy `supabase/after_setup_create_admin.sql` → đăng nhập.
- **S1 (Danh mục & Xe) — XONG (chưa push)**: `supabase/migrations/0006_catalog.sql` (bảng oil_types/suppliers/transaction_types/tanks/vehicles deny-by-default + RPC list/active/upsert/toggle, gated `catalog:manage`; đọc active gated `catalog:read` cho mọi vai trò; seed 3 loại giao dịch) + màn "Danh mục & Xe" trong `public/index.html` (5 tab: Téc/Xe/Loại dầu/NCC/Loại giao dịch, thêm/sửa/bật-ngừng). Loại giao dịch cố định (chỉ đổi tên/ngừng). Đã qua kiểm thử tĩnh (`node --check` FE, cấu trúc SQL). **Cần push + deploy để validate DB thật.**
- **S2 (Sổ cái + vòng đời phiếu bơm) — XONG (chưa push)**: `supabase/migrations/0007_pump_ledger.sql` — bảng `ledger` append-only (entry_type bom/nhap/adjust; km_run generated; status Nhap/ChoDoiChieu/DaDuyet), deny-by-default. Máy trạng thái: Nhap→(Submit ngày, cấp Số phiếu BOM-YYYY-######)→ChoDoiChieu→DaDuyet | Lệch→về Nhap+reject_reason. RPC: create/update/delete/submit_day/withdraw/list_mine + review_queue/approve/reject (chặn tự-duyệt: created_by<>auth.uid()), vehicle_last_km (tự điền KM cũ). FE: màn Nhập bơm (form card + danh sách phiếu của tôi + Submit ngày + rút lại) và Đối chiếu chéo (hàng đợi Khớp/Lệch). Test mô phỏng `supabase/tests/pump_flow_simulation.sql` (BEGIN…ROLLBACK, giả lập auth.uid qua request.jwt.claim.sub) — **chưa chạy thật (sandbox không có DB); chạy trong SQL Editor sau deploy.** Kiểm thử tĩnh FE/SQL đã qua.
- **S3 (Ảnh chứng từ + OCR-ready) — XONG (chưa push)**: `supabase/migrations/0008_pump_photos.sql` — bảng `pump_photos` (kind pump_meter/odometer; storage_path; trường OCR ocr_value/ocr_confidence/manually_corrected rỗng V1) deny-by-default + bucket Storage RIÊNG TƯ `chung-tu` + policy storage.objects (đọc: nhân sự Hoạt động; ghi/xóa: quyền `pump:create`, qua helper SECURITY DEFINER `chungtu_can_read/upload` tạo ở migration). **Bucket + policy KHÔNG đặt trong migration** (vai trò `supabase db push` không sở hữu `storage.objects` → `create policy` lỗi "must be owner" và hỏng cả push); tạo MỘT LẦN qua Dashboard — xem `supabase/storage_chungtu_setup.md`. RPC `rpc_pump_photo_add/delete/list` (chỉ người tạo, chỉ khi Nháp; path phải thuộc `<ledger_id>/…`). **Override `rpc_pump_submit_day`**: chặn submit nếu thiếu ảnh đồng hồ bơm (mọi phiếu) hoặc thiếu ảnh công-tơ-mét khi Xe có công-tơ-mét — validate cả lô trước khi cấp Số phiếu. FE: card Nhập bơm sau khi lưu chuyển sang chế độ sửa, hiện panel "Ảnh chứng từ" (chụp camera `capture=environment`, upload Storage → đăng ký metadata, xem/xóa qua URL ký); màn Đối chiếu có nút "Ảnh" mở modal xem 2 ảnh. Test `supabase/tests/pump_photo_flow.sql` (gating + ranh giới RPC); cập nhật `pump_flow_simulation.sql` seed ảnh trước submit. Kiểm thử tĩnh FE/SQL đã qua; **chạy test trong SQL Editor sau deploy.**
- **S4 (Phiếu nhập NHAP) — XONG (chưa push)**: `supabase/migrations/0010_nhap_receipt.sql` — thêm cột `ledger.supplier_invoice_no` (Số HĐ NCC, nhập tay ≠ Số phiếu), mở `pump_photos.kind` thêm 'receipt'. RPC `rpc_nhap_*` (create/update/delete/submit_day/withdraw/list_mine/review_queue/approve/reject) + ảnh `rpc_nhap_photo_add/delete/list` cho entry_type='nhap', **tái dùng nguyên máy trạng thái + đối chiếu chéo + bucket 'chung-tu' + helper Storage của S2/S3**. Vai trò dùng CHUNG phiếu bơm: tạo `pump:create`, đối chiếu bởi ThuKho KHÁC `pump:review` (chặn tự-duyệt). Submit chặn nếu thiếu ≥1 ảnh 'receipt'; cấp Số phiếu `NHAP-YYYY-######`. FE: màn "Phiếu nhập" (NCC/téc/loại dầu/lít/đơn giá/**thành tiền tự tính**/Số HĐ NCC + panel ảnh + danh sách + Submit ngày; chọn téc gợi ý loại dầu của téc) và "Đối chiếu nhập" (hàng đợi Khớp/Lệch + xem ảnh). Test `supabase/tests/nhap_flow_simulation.sql`. Kiểm thử tĩnh FE (`node --check`)/SQL đã qua; **chạy test trong SQL Editor sau deploy.**
- **S5 (Tồn kho thời gian thực) — XONG (chưa push)**: `supabase/migrations/0011_inventory.sql` — bảng `tank_opening` (tồn đầu gốc/téc) + `rpc_tank_opening_set` (KeToan, `adjust:manage`) + `rpc_inventory_stock` (`inventory:read`) tính **Tồn cuối = Tồn đầu + Nhập − Xuất** (chỉ bút toán `DaDuyet`/`legacy`; Nhập=entry_type 'nhap', Xuất=entry_type 'bom' kind 'xuat', bơm ngoài tank_id null không trừ). Cột `adjust`=0 — **S6 sẽ OVERRIDE `rpc_inventory_stock`** cộng/trừ tịnh có dấu. FE màn "Tồn kho" (bảng + thanh %đầy, cờ Tồn âm/Vượt sức chứa/Dưới điểm đặt hàng; panel "Đặt tồn đầu gốc" chỉ KeToan). KM auto-fill/cảnh báo đã có từ S2. Test `supabase/tests/inventory_stock_simulation.sql` (tồn cuối=7200). Kiểm thử tĩnh FE/SQL đã qua; **chạy test + seed tồn đầu thực (S9) sau deploy.**
- **S6 (Tịnh téc / Kiểm kê + phân bổ) — XONG (chưa push)**: `supabase/migrations/0012_stock_period.sql` — bảng `stock_period` (kỳ chung tịnh téc/kiểm kê) + `stock_period_alloc` (phân bổ/xe). Helper `tank_book_before`. RPC `rpc_stock_period_create/update/delete/close/list/detail` (`adjust:manage`, KeToan, KHÔNG đối chiếu). CHỐT: diff=book−thực tế → tịnh âm/dương; phân bổ `PB=lít_xe×tịnh/tổng_lít` (round 2, dồn dư vào xe lít lớn nhất → tổng khớp tuyệt đối); cấp Số kỳ `TINH-YYYY-######`. **OVERRIDE `rpc_inventory_stock`** cộng −Σtịnh âm +Σtịnh dương vào Tồn cuối (thêm cột tinhAm/tinhDuong). FE màn "Tịnh téc / Kiểm kê" (tạo/danh sách/Chốt nhập tồn thực tế/xem Phân bổ). Test `supabase/tests/stock_period_simulation.sql` (3 xe, tịnh âm 100, tổng khớp). Kiểm thử tĩnh FE/SQL đã qua; **chạy test trong SQL Editor sau deploy.**
- **S7 (Cảnh báo reorder + Web Push) — XONG (chưa push)**: `supabase/migrations/0013_notifications.sql` — bảng `notification` (ton_thap/ton_am/vuot_suc_chua, dedup_key, pushed_at) + `notification_read` + `push_subscription`; quyền `alert:read` (KeToan). `alerts_scan()` (nội bộ) tính lại tồn từng téc (đúng công thức S5/S6) → chèn cảnh báo **dedup kind:tank:ngày**; `rpc_alerts_scan` (inventory:read), `rpc_alerts_list`/`rpc_alerts_mark_read` (alert:read), `rpc_push_subscribe/unsubscribe`. `push_dispatch()` gọi Edge Function qua `net.http_post` (URL+secret ở `app_config`) — **guard**: thiếu cấu hình/pg_net ⇒ bỏ qua an toàn. Edge Function `supabase/functions/send-push` (VAPID, `npm:web-push`) xác thực `x-webhook-secret`, đẩy push tới KeToan/Admin, set pushed_at. FE: màn **Cảnh báo** + chuông header (đếm chưa đọc) + bật/tắt push; quét nền khi mở Tồn kho/đăng nhập; `sw.js` push+click. Cấu hình `supabase/push_setup.md` + workflow `deploy-functions.yml`. Test `supabase/tests/alerts_simulation.sql`. Kiểm thử tĩnh FE/SQL đã qua; **cần cấu hình VAPID/secret + chạy test sau deploy.**
- **S8 (Báo cáo tiêu hao tháng + Excel) — XONG (chưa push)**: `supabase/migrations/0015_report_monthly.sql` — `report_month_agg(d0)` (nội bộ) gộp theo xe: diesel theo từng téc, bơm ngoài, dầu phụ theo loại, KM (Σ km_run), tịnh âm/dương (phân bổ kỳ chốt trong tháng). `rpc_report_monthly(month,year)` (report:read) trả cột téc động + dầu phụ + dòng theo xe (Tổng diesel, Tiêu hao thực = Tổng diesel + tịnh âm − tịnh dương, L/100km + L/100km tháng trước) + TỔNG. FE màn "Báo cáo tháng" (chọn tháng/năm, bảng, ⬇ Tải Excel qua SheetJS `xlsx.full.min.js`). Tái hiện SUMIFS của TONG HOP TIEU HAO. Regression số học so file gốc chạy **sau** khi deploy 0015+0016 (S9 nạp dữ liệu).
- **S9 (Migrate dữ liệu lịch sử) — XONG (chưa push)**: `supabase/migrations/0016_migrate_legacy.sql` (sinh tự động từ Excel; **đánh số 0016** vì 0015 đã deploy trước — migration phải tiến, không lùi). **DATA_GOC thực chỉ 1.725 dòng** (5997 là dải filter), khoảng **1/1–6/7/2026**. Widen `ledger.liters→numeric(14,3)`; bảng `_legacy_data_goc` + 1.725 dòng; transform → `ledger` (legacy, DaDuyet) resolve tên qua `normalize_text`; nguồn téc vật lý → tank_id, dầu phụ/bơm ngoài → null. Seed Tồn đầu 1/1/2026 (Téc2=104, Téc3=5779; Téc1/cát nghiền=0) + 18 kỳ tịnh cũ (DaChot) + phân bổ tịnh. Idempotent qua `app_config.legacy_migrated`. Verify `supabase/tests/migrate_verify.sql`. **⇒ Dữ liệu legacy vào DB khi 0016 push lên `main` (Action `supabase db push`).**
- **Bài học migration order**: đừng merge slice có số nhỏ SAU slice số lớn hơn đã áp dụng. S8=0015 đã deploy trước S9 nên S9 phải đổi 0014→0016.
- **Đợt nâng cấp FE (chỉ frontend, KHÔNG đụng backend/RPC/schema)** — nguồn: `docs/PRD_frontend_uplift.md`, issue `docs/issues/S10..S12`. Đồng bộ vỏ + cảm quan app tham chiếu *Mua hàng*. Giữ nguyên hợp đồng seam: mọi `renderXxx(el)` render vào `#screen`.
  - **S10 (Vỏ + Design system + Chuông + Modal) — XONG (branch `s10-fe-vo-va-design-system`, commit 172c7ab, chưa push)**: thay pill-nav bằng drawer hamburger (đánh số/nhóm gập/sắp theo vai trò qua `ROLE_MENU_ORDER`+`orderScreensForRole`) + thanh tab dưới (4 màn + "Thêm") + FAB back/to-top (`navTo`+history.pushState/popstate). Design tokens đầy đủ + component CSS (`.card/.notice/.pill/.metric/.totals/.workspace/.loading-line/.spin`, toast ok/err). Header logo (png+fallback svg) + sticky backdrop-blur. Chuông = badge + `.notif-panel` **tái dùng RPC S7** (`rpc_alerts_list`/`rpc_alerts_mark_read`, KHÔNG RPC mới). Helper `withBusy()`/`loadingText()`. Modal `openModal/modalErr/promptReason` (thay `prompt()` cho lý do trả về phiếu ở Đối chiếu). `.workspace` 2 cột → 1 cột mobile. Bỏ emoji còn sót (no-emoji-in-ui). Kiểm thử tĩnh `node --check` + QA preview 375/768/1280.
  - **S11 (Tự xếp bảng thành thẻ mobile) — XONG (branch `s11-fe-bang-xep-the-mobile` nhánh từ s10, commit 254cc4c, chưa push)**: `labelTableCells(root)` gắn `data-label` từ `<thead th>`; `MutationObserver` trên `#screen` tự gắn nhãn cho bảng render bất đồng bộ (chỉ theo dõi childList → không tự lặp). CSS ≤760px: `table:not(.line-table)` → thẻ (ẩn thead, hàng=card, ô hiện nhãn cột trái + giá trị phải); `.line-table` → xếp dọc ô nhập full-width; desktop/tablet giữ hàng-cột. **Lưu ý**: observer chạy bằng microtask (không dùng requestAnimationFrame — bị throttle khi tab ẩn). QA preview 375px (xếp thẻ) / 1280px (giữ table-row).
  - **S12 (PWA polish) — XONG (branch `s11-fe-bang-xep-the-mobile`, chưa commit/push)**: sinh bộ icon từ `Logo.png` bằng `sips` → `public/icon-192.png`, `icon-512.png`, `icon-maskable-512.png` (logo trên nền trắng; maskable thu nhỏ vào safe-zone). `manifest.webmanifest` đầy đủ (icons 192/512/maskable, `lang:vi`, `scope`, `orientation:portrait`, `background_color:#F4F6FA`, `theme_color:#0E5AA7`) — thay `icons:[]`. `apple-touch-icon`→`icon-192.png` (các meta apple/mobile đã có từ S10). `sw.js` **network-first** cùng-origin (cache `tn-fuel-v1` precache vỏ app; có mạng lấy bản mới + cập nhật cache; mất mạng phục vụ cache, navigate fallback `index.html`); **BỎ QUA request khác origin** (Supabase/CDN đi thẳng). **GIỮ nguyên** handler `push`+`notificationclick` S7 — bổ sung: notificationclick đưa về **màn Cảnh báo** (`./#alerts` cho cửa sổ mới; `postMessage{open-screen:alerts}` cho client đang mở). FE: `bootstrap` đọc `location.hash` để deep-link đúng màn nếu được phép; listener `serviceWorker message` → `navTo`. Kiểm thử tĩnh `node --check` sw.js + JSON manifest + inline script OK; QA preview: manifest hợp lệ, 3 icon đúng kích thước, SW active, precache đủ 9 file vỏ, không lỗi console.
- **Lộ trình S0–S9 hoàn tất (chưa push S7/S8/S9). FE uplift: S10+S11+S12 xong (chưa push).** Việc còn lại: push các nhánh + deploy + cấu hình push (S7) + chạy verify (S8/S9) trên DB thật.
- **Đợt sửa tính năng T7/2026 (7 batch, branch `s11-fe-bang-xep-the-mobile`)** — user yêu cầu, làm từng batch để feedback loop liên tục:
  - **Batch 1 (điều hướng) — XONG (commit 817620e)**: `ROLE_MENU_ORDER` mới ưu tiên Bơm→Nhập→Tồn kho→Tịnh téc→Báo cáo→Đối chiếu bơm; thêm `MENU_HIDDEN=['nhapReview','alerts']` + helper `menuScreens()` (ẩn khỏi drawer/tab nhưng vẫn truy cập được). **Quyết định**: bỏ Đối chiếu nhập (Phiếu nhập duyệt thẳng khi Submit — sẽ hiện thực ở Batch 3); giữ chuông + push, chỉ ẩn màn Cảnh báo khỏi menu.
  - **Batch 2 (UX nhập liệu + ngày) — XONG (commit tiếp theo)**: CSS `.req` (nền vàng nhạt) + `label .rq` (dấu *) + `.fnote` (dòng hướng dẫn). Áp cho **form Bơm + form Nhập**: mọi trường có dòng note; trường bắt buộc highlight vàng; thêm ô **Ngày** (mặc định hôm nay, dùng `entryDate` prefill khi sửa). Migration **`0017_entry_date_editable.sql`**: thêm `p_entry_date` cho `rpc_pump_update`/`rpc_nhap_update` (drop chữ ký cũ + tạo lại; create đã có sẵn p_entry_date từ S2/S4). Các form còn lại (Danh mục/Tịnh téc/Tồn đầu/Tài khoản) sẽ áp cùng pattern khi đụng ở Batch 4/5. QA preview: date=hôm nay, Bơm 7 req/10 note, Nhập 5 req/9 note, không lỗi console.
  - **Batch 3 (ảnh nhập + duyệt thẳng + storage tháng) — XONG (commit c4fbe36)**: migration **`0018`** — `pump_photos` thêm kind `bien_ban_giao_nhan`/`phieu_nhap_kho`; `rpc_nhap_photo_add` nhận `p_kind` (2 loại bắt buộc, drop chữ ký cũ); `rpc_nhap_photo_list` trả 2 loại (giữ 'receipt' cũ); `rpc_nhap_submit_day` chặn thiếu 1 trong 2 ảnh + **DUYỆT THẲNG** (status='DaDuyet', reviewed_by=self) — bỏ đối chiếu chéo cho phiếu nhập (phiếu bơm giữ nguyên). Validate path ảnh đổi sang **kiểm tra segment** `position('/'||ledger_id||'/' in '/'||path||'/')>0` (chấp nhận cả path cũ `<lid>/...` lẫn mới `<YYYY-MM>/<lid>/...`) — áp cho cả `rpc_pump_photo_add`. FE: helper `monthOf`/`photoPath`; ảnh bơm + nhập lưu `<YYYY-MM>/<ledgerId>/<kind>-<ts>.<ext>`; `renderReceiptSection` 2 slot bắt buộc; submit toast "Đã nhập & duyệt N phiếu vào tồn". **Màn Đối chiếu nhập (nhapReview) đã ẩn từ B1, RPC review nhap còn nhưng không dùng.**
  - **Batch 4 (tồn kho) — XONG (commit b85390a)**: migration **`0019`** — `tanks.clamp_negative` (bật cho cát nghiền qua `normalize_text(name) like '%cat nghien%'`) → tồn cuối kẹp `greatest(.,0)`; OVERRIDE `rpc_inventory_stock` trả thêm `avgDailyWeek`/`avgDailyMonth` (Σ xuất 7/30 ngày ÷ 7/30), `daysLeft` (=tồn cuối÷avgMonth), `leadTimeDays` (=tanks.lead_time_days), `minStock` (=avgMonth×lead). FE bảng Tồn kho thêm 5 cột + cờ **"Cần đặt hàng ngay"** khi `daysLeft ≤ leadTimeDays`; form Đặt tồn đầu áp pattern bắt buộc/hướng dẫn.
  - **Batch 5 (danh mục xóa cứng) — XONG (commit tiếp theo)**: migration **`0020`** `rpc_catalog_delete(kind,id)` (catalog:manage) — chặn xóa nếu còn tham chiếu (ledger/stock_period/stock_period_alloc/tanks.oil_type) → báo dùng "Ngừng"; loại giao dịch KHÔNG cho xóa; audit. FE: nút **Xóa** (đỏ) ở mọi tab trừ Loại giao dịch, gọi `catDelete`.
  - **Batch 6 (Admin sửa phiếu + audit) — XONG (commit c661d9c)**: migration **`0021`** — `ledger.edited_by/edited_at`; gate `'ledger:admin'` (Admin bỏ qua ma trận quyền ⇒ chỉ Admin qua). `rpc_admin_ledger_list(days,type,q)` (dashboard N ngày, lọc loại + tìm số phiếu/biển số/NCC/ghi chú), `rpc_admin_ledger_update` (sửa thẳng mọi trạng thái, validate nhẹ liters≥0, đóng dấu edited_by/at + audit), `rpc_admin_ledger_delete` (xóa + audit), `rpc_admin_ledger_audit` (lịch sử phiếu từ audit_log). FE: màn **"Sửa phiếu (Admin)"** (SCREENS + ROLE_MENU_ORDER Admin) — bảng phiếu + nút Sửa (modal theo entry_type dùng options từ state.active) / Lịch sử (modal) / Xóa. QA preview: dashboard + modal sửa nhap prefilled OK.
  - **Batch 7 (3 báo cáo tiêu hao) — XONG (commit tiếp theo)**: migration **`0022`** — `rpc_report_diesel_daily` (↔ TIEU HAO DIESEL_2: lưới diesel xuất nội bộ theo ngày × xe + tổng tháng: tongXuat/bomNgoai/tinhAm/tinhDuong/tieuHaoThuc/km/lPer100/lPerKm/lPerKmPrev) và `rpc_report_diesel_detail` (↔ TIEU HAO DIESEL_1: theo xe, Định mức=pump_norm vs L/100km thực tế + chênh lệch). Dùng lại `report_month_agg` (S8) cho nhất quán. FE màn Báo cáo: **3 tab** (Tổng hợp tiêu hao=rpc_report_monthly S8 | Diesel theo ngày | Diesel chi tiết) + chọn tháng/năm + **Tải Excel** riêng từng loại (SheetJS). Bảng báo cáo dùng class **`.rpt-grid`** để cuộn ngang trên mobile (thêm `:not(.rpt-grid)` vào quy tắc S11 để không tự xếp thẻ). **Lưu ý**: DIESEL_1 gốc có cột nhập tay (định mức ca, đọc công-tơ-mét) không có trong data model — bản này thay bằng plan-vs-actual dựa trên pump_norm. QA preview 3 tab render + Excel OK, không lỗi console.
  - **Batch 8 (v11 data + vai trò + gắn cờ + UX đính ảnh) — XONG (branch `s11-fe-bang-xep-the-mobile`, chưa push)**:
    - **Dữ liệu v11**: `File nhap lieu xang dau_v11.xlsx` mới (đổi tên từ bản `(1)`) có **KM/công-tơ-mét cho 478 dòng / 47 xe**. Migration **`0024_migrate_legacy_v11.sql`** (sinh từ Python, tái dùng nguyên logic transform/opening/18 kỳ tịnh của `0016`) **GHI ĐÈ SẠCH**: xóa mọi bút toán/kỳ/tồn-đầu `legacy` cũ rồi nạp lại 1.738 dòng DATA_GOC **kèm km_old/km_new**. Guard cờ `legacy_migrated_v11` (chạy được dù `0016` đã set `legacy_migrated`). ⇒ `rpc_vehicle_last_km` trả đúng KM cũ để form tự điền.
    - **Vai trò + luồng**: migration **`0023_roles_review_flow.sql`** — thêm vai trò **`TruongBoPhan`** (nới `profiles_role_check` + recreate `rpc_admin_create_user/update_user`). Ma trận quyền: ThuKho bỏ `pump:review` (thấy mục 1,2,3); TBP + KeToan (kế toán tổng hợp) có `ledger:flag`+adjust/report/catalog/inventory (mục 3,4,5,6 + Danh mục); Admin thấy hết (mục 7). **Phiếu bơm DUYỆT THẲNG** khi Submit (override `rpc_pump_submit_day` → DaDuyet, giữ kiểm tra ảnh) — bỏ đối chiếu chéo của thủ kho. **Số phiếu theo ngày** `PREFIX-YYYYMMDD-##` (`next_code_for_date`, dùng cho cả pump+nhap submit). Cột cờ trên `ledger` (`flagged/flag_reason/flagged_by/flagged_at/flag_status`). RPC mới: `rpc_ledger_flag` (mục 6 gửi Admin), `rpc_review_ledger_list`+`rpc_review_photo_list` (TBP xem phiếu duyệt + ảnh), override `rpc_admin_ledger_list` (+`p_flagged_only`+trường cờ), `rpc_admin_flag_resolve`.
    - **FE `public/index.html`**: (a) `ROLE_LABELS`/`ROLE_MENU_ORDER` + option Trưởng bộ phận ở Tài khoản; (b) **Đính ảnh trong CÙNG thẻ** — ô ảnh nằm ngay trong form Bơm/Nhập, ảnh **staged** (`state.pumpStage`/`state.nhapStage`) rồi **một nút Lưu** tạo phiếu + upload ảnh cùng lúc, xong reset form; danh sách "Phiếu đã lưu" **bấm để mở chi tiết + ảnh** (`pumpDetail`/`nhapDetail`); (c) **Mục 6 `renderReview`** viết lại = duyệt phiếu đã duyệt + xem ảnh (`rpc_review_photo_list`) + **"Yêu cầu Admin sửa"** (`rpc_ledger_flag`); (d) **Mục 7** thêm lọc "chỉ phiếu bị gắn cờ" + badge/lý do + nút "Bỏ cờ", sửa phiếu xong tự resolve cờ.
    - **QA preview mock (chưa DB thật)**: menu đúng 4 vai trò; form Bơm ô ảnh inline (2 slot có công-tơ-mét / 1 slot không), nút "Lưu phiếu"; chi tiết phiếu + modal ảnh; mục 6 badge "Đã gửi Admin" + nút "Yêu cầu Admin sửa"; mục 7 checkbox cờ + "Chờ sửa" + lý do; `node --check` OK, không lỗi console.
  - **⇒ Đợt sửa tính năng T7/2026 (7 batch) + Batch 8 XONG (branch `s11-fe-bang-xep-the-mobile`, đã push tới B5; B6/B7/B8 cần push).**
  - **Batch 9 (tài khoản cá nhân + album/watermark + xem nháp chéo) — XONG (branch `s9-migrate-v2`, chưa push)**:
    - **Mục 1 — tài khoản cá nhân + đổi PIN**: migration **`0032_self_account_unit.sql`** — thêm cột `profiles.unit` (Đơn vị); `rpc_bootstrap` trả thêm `unit`; `rpc_admin_create_user`/`rpc_admin_update_user` nhận `p_unit` (drop chữ ký cũ 4-arg/(uuid,3×text) → tạo lại có p_unit default null); `rpc_admin_list_users` trả unit. FE: màn **`myAccount`** ("Tài khoản của tôi", quyền null → mọi vai trò, ẩn khỏi menu, mở qua click `#userBadge`) — xem hồ sơ + **đổi PIN** (xác thực PIN cũ bằng `signInWithPassword` rồi `sb.auth.updateUser({password:'tn-pin::'+pin})`, KHÔNG cần RPC). Màn Tài khoản (Admin) thêm ô Đơn vị khi tạo + cột Đơn vị + nút "Đơn vị" (prompt) sửa đơn vị tài khoản đã có.
    - **Mục 2 — chọn ảnh từ album + watermark**: bỏ `capture="environment"` ở MỌI ô ảnh (bơm/nhập/kiểm kê) → cho chọn cả camera lẫn album. Helper FE `watermarkImage(file)` — vẽ canvas, đóng dấu 2 dòng (thời điểm nộp · tên+đơn vị người nộp qua `watermarkLines()` đọc `state.user`), nén JPEG cạnh dài ≤1600px; áp cho `uploadStagedPumpPhotos`/`uploadStagedNhapPhotos`/upload biên bản kiểm kê. `photoPath` giờ luôn `.jpg`. Dấu-vết-thật vẫn là `pump_photos.created_by/created_at` (server) — watermark chỉ là bản in bằng mắt. Đã **verify watermarkImage chạy thật trong preview** (xuất JPEG 800×600); 1 dòng khi chưa login (state closure), 2 dòng khi có `state.user`.
    - **Mục 3 — thủ kho xem chéo nháp, chỉ sửa nháp của mình**: migration **`0033_drafts_review.sql`** — `rpc_drafts_pending(p_entry_type)` (gate `pump:create`) liệt kê MỌI phiếu `status='Nhap'` (bom+nhap) của mọi thủ kho + `createdByName` + `canEdit`(=created_by=mình) + `photoKinds` (biết thiếu ảnh); `rpc_draft_photo_list(id)` xem ảnh nháp bất kỳ (chỉ đọc). "Chỉ sửa của mình" ĐÃ ép sẵn ở 0008/0018 (update + photo add/delete chỉ `created_by=auth.uid()` & status Nhap) — 2 RPC này chỉ THÊM đọc. FE: màn **`drafts`** ("Phiếu nháp trong ngày", ThuKho+Admin) — bảng nháp; của mình → "Sửa" (set `pumpEdit/nhapEdit` + `navTo` tái dùng form sửa sẵn có, upload lại ảnh); của người khác → "Xem" (modal chi tiết + ảnh, chỉ đọc). **Lưu ý**: Batch 8 đã cho Submit = duyệt thẳng, mỗi thủ kho chỉ submit phiếu của mình ⇒ màn này là soi chéo bằng mắt, KHÔNG phải cổng duyệt.
  - **CHƯA chạy trên DB thật**: migration 0017–0024 + **0032–0033** + FE mới chỉ kiểm thử tĩnh (`node --check`, cấu trúc `$$`) + QA preview mock. Cần push→deploy→chạy test SQL để validate. Sau deploy 0024: `select count(*) from ledger where legacy` ⇒ 1738; spot-check `rpc_vehicle_last_km` vài xe (vd 28C01544 ⇒ 479520). Sau 0032: kiểm `rpc_bootstrap` trả `unit`; sau 0033: `rpc_drafts_pending` trả nháp + `canEdit` đúng theo người gọi.

## 4. Lộ trình lát (docs/issues/)
S0 nền tảng ✅ · S1 danh mục & xe ✅ · S2 sổ cái + vòng đời phiếu bơm ✅ · S3 ảnh + OCR-ready ✅ · S4 phiếu nhập ✅ · S5 tồn kho realtime ✅ · S6 tịnh téc/kiểm kê + phân bổ ✅ · S7 cảnh báo + push ✅ · S8 báo cáo tháng ✅ · S9 migrate ✅.

**Đợt nâng cấp FE (docs/PRD_frontend_uplift.md):** S10 vỏ + design system + chuông + modal ✅ · S11 bảng tự xếp thẻ mobile ✅ · S12 PWA polish ✅.

## 5. Lưu ý kỹ thuật
- KHÔNG sửa migration cũ; thêm `NNNN_*.sql` mới. Giữ RPC-first (xem AGENTS.md).
- Kiểm thử tĩnh: `node --check` cho FE; kiểm tra cấu trúc SQL (sandbox không có psql/pglast/mạng). Validate DB thật khi deploy.
- Logic gốc để tra cứu: `File nhap lieu xang dau_v11.xlsx` (sheet BOM/NHAP/TINH_TEC/TON_KHO/DATA_GOC/DANH_MUC/_TON_CAPACITY/_DM_BOM_BOM) + `appscript code.rtf` (công thức TON_KHO_ORIGINAL_FORMULAS_V20, phân bổ tịnh rows 233+, báo cáo restoreTieuHao*).
- Số phiếu tự sinh: helper `next_code_year('BOM')` → BOM-2026-000123 (đã có trong 0001).
- Tồn đầu gốc từng téc seed tại 1/1/2026 (lấy giá trị đo tay từ file cũ) — làm ở S5/S9.

## 6. Bảo mật
User đã lỡ dán service_role/secret/DB password + 1 GitHub token vào chat trước đó → đã khuyến nghị revoke/rotate. Không nhúng khóa nhạy cảm vào code/commit; chỉ anon+publishable ở frontend, service_role/secret trong Supabase secrets.
